import React from 'react';
import Login from './Login/login';
import Register from './Register/register';

const test = (props) => {
  return (
    <div>
      <Login/>
      <Register/>
    </div>
  );
}

export default test;
